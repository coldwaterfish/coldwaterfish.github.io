### Date

```js
'使用从UTC1970年1月1日零时开始经过的毫秒数来保存日期',
    'var now=new Date()，调用构造函数不加参数时，获得当前的日期和时间',
    'var year=now.getYear()/getMonth()+1/getDate()/getHours()/getMinutes()/getSeconds()',
    'toDateString()/toTimeString()/toLocalDateString()/toLocalTimeString()/toUTCString()',
    'moment库：http://momentjs.cn/'
```

